namespace Shared
{
    // Add 'distance' in front of each enum to generate valid message
    public enum Direction
    {
        FromCurrent,
        N,
        NE,
        E,
        SE,
        S,
        SW,
        W,
        NW,
    }
}