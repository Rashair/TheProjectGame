namespace Shared
{
    public enum Team { Red, Blue };
    public enum MessageID { CheckPiece = 1, PieceDestruction, Discover, GiveInfo, BegForInfo, JoinTheGame, Move, Pick, Put };
    public enum Directions { N, S, E, W};
}