namespace Shared
{
    public enum Team { Red, Blue };
    public enum MessageID { CheckPiece = 1, PieceDestruction, Show, GiveInfo, BegForInfo, JoinTheGame, Move, Pick, Put };
    public enum Directions { N, S, E, W};
    public enum GoalAreaInformations { IDK, N, G };
}